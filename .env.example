# FastAPI runtime environment name
APP_ENV=development

# Python logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# API/application semantic version exposed by /api/version
APP_VERSION=0.1.0

# Relational database connection string for schema/models work
DATABASE_URL=sqlite:///./inventory.db
