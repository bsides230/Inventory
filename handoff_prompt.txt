## Task Handoff: Order Viewer Extraction

**Context:**
The Order Viewer component has been successfully extracted from the main Admin Panel (`web/admin.html` & `web/admin.js`) into its own standalone, authenticated page (`web/viewer.html` & `web/viewer.js`).
- A link to the new "Order Viewer" page (with a notepad `clipboard-list` icon) was added to the top navigation bar of the `admin.html` page.
- The new `viewer.html` features a responsive top navigation bar containing a "Back to Admin" button, an "Order Selector Dropdown" replacing the old list view, language/theme toggles, and the main order viewing/editing area.

**Current Blocker:**
The backend file (`server.py`) is missing from the repository, preventing local verification and complete backend routing setup.

**Next Steps for the Next AI Task:**
1.  **Upload/Restore `server.py`:** Ensure the FastAPI application file (`server.py`) is present in the repository root.
2.  **Add Backend Route:** Add a new GET route in `server.py` to serve the `web/viewer.html` file.
    ```python
    @app.get("/viewer.html")
    async def get_viewer_html():
        return FileResponse(os.path.join("web", "viewer.html"))

    # Optional: You may also want to alias /viewer to serve viewer.html
    @app.get("/viewer")
    async def get_viewer_alias():
        return FileResponse(os.path.join("web", "viewer.html"))
    ```
    *Note: Adjust the exact `FileResponse` path based on how the existing `web/` static files and HTML files are currently served in `server.py`.*
3.  **Start Server & Verify:** Run `bash start.sh` to start the backend.
4.  **Frontend Verification:** Use Playwright to verify the UI changes:
    *   Navigate to `/admin`, login, and click the "Order Viewer" icon in the top right.
    *   Ensure `/viewer.html` loads correctly and handles authentication (redirects to `/admin` if not logged in).
    *   Test the "Order Selector Dropdown" in the top bar of the viewer page to ensure orders load and display correctly.
    *   Test the "Back to Admin" button in the viewer page.
5.  **Clean up:** Remove this `handoff_prompt.txt` file once the above steps are completed.
